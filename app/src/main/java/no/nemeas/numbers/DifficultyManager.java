package no.nemeas.numbers;

public class DifficultyManager {
    public static Setup getSetup(int completedStages, int failedStages, int lvl) {
        // TODO - implement
        return new Setup(5, 0, 10);
    }
}
